/**
 * AmbientGradient — neutered to avoid conflicting with the Stitch design system background.
 * The diagonal texture is applied globally in index.css.
 */
export default function AmbientGradient() {
  return null;
}
